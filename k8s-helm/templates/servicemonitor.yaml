apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pulse-app-monitor
  labels:
    release: monitor # Должно совпадать с именем вашего helm-релиза мониторинга
spec:
  namespaceSelector:
    matchNames:
      - pulse-prod
  selector:
    matchLabels:
      app: pulse-app
  endpoints:
  - port: http-backend
    path: /metrics